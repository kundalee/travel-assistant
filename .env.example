# 複製為 .env.local 後修改（.env.local 不納入版本控制）

# 真正後端的網址。不設定 → 所有 API 由瀏覽器內的假後端回應
# VITE_API_BASE_URL=https://api.example.com

# 逐步上線：只有列出的群組送往真正的後端，其餘仍用假後端。
# 群組為各資源檔 resource() 的第一個參數（見 docs/ENDPOINTS.md），例如 auth、traveler.profile；「traveler」代表整個團員入口。
# 建議先切換 auth，其他群組才能使用真正後端發出的登入 token。
# VITE_API_REAL=auth,traveler.profile
